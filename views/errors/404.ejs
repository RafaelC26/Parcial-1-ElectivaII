<%- include("../partials/head") %>
<%- include("../partials/sidebar") %>

<div class="main">
  <%- include("../partials/topbar") %>

  <main class="content">
    <section class="card">
      <div class="empty-state" style="padding:var(--space-16) var(--space-6)">
        <div class="empty-state__icon" style="width:64px;height:64px;font-size:var(--text-2xl);font-weight:600;color:var(--text-muted)">
          404
        </div>
        <h1 class="empty-state__title" style="font-size:var(--text-xl)">
          No encontramos esta página
        </h1>
        <p class="empty-state__text">
          La dirección solicitada no existe o fue modificada.
        </p>
        <a href="/" class="btn btn--primary">
          <i data-lucide="layout-dashboard"></i>
          Volver al dashboard
        </a>
      </div>
    </section>
  </main>
</div>

<%- include("../partials/footer") %>
